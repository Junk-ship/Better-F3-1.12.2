### This mod removes most of the unnecessary features from the F3 menu, so it doesn't take up the entire screen. Instead, the essential information is displayed in the top left corner when F3 is pressed.
